name: Template Validator

on:
  workflow_call:
    inputs:
      dummy_input:
        description: 'Optional dummy input'
        required: false
        type: string
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Validate Template
        run: |
          echo "Running template validation..."
          # Simulate some checks (for example, verifying required sections exist)
          echo "Template validated successfully."
